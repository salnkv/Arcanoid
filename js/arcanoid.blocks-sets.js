function BlocksSets() {
	var colorsDefault = ["green", "yellow", "orange", "red"];
	var colorsMario = ["#CCC", "#F4A350", "#A6801D", "#EB3323"];
	var maxNumberOfSet = 2;

	this.getMaxNumber = function() {
	    return maxNumberOfSet;
	}

	this.getBlockSet = function (blockWidth, blockHeight, blocksTop, number) {
		if (number) {
		    if (number < 1)
		    	number = 1;
		    else if (number > maxNumberOfSet) {
		        number = maxNumberOfSet;
		    }
		} else {
		    number = 1;
		}

	    number -= 1;

		return [
			[
        new Block(1 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(2 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(3 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(4 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(5 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(6 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(7 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(8 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(9 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(10 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(1 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(2 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(3 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(4 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(5 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(6 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(7 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(8 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(9 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(10 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(1 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(2 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(3 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(4 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(5 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(6 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(7 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(8 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(9 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(10 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(1 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(2 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(3 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(4 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(5 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(6 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(7 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(8 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(9 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(10 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(1 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(2 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(3 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(4 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(5 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 4)),
        new Block(6 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 4)),
        new Block(7 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(8 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(9 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(10 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(1 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(2 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(3 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(4 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(5 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 4)),
        new Block(6 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 4)),
        new Block(7 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(8 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(9 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(10 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(1 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(2 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(3 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(4 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(5 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(6 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(7 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(8 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(9 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(10 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(1 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(2 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(3 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(4 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(5 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(6 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 3)),
        new Block(7 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(8 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(9 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(10 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(1 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(2 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(3 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(4 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(5 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(6 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(7 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(8 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(9 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(10 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(1 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(2 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(3 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(4 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(5 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(6 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 2)),
        new Block(7 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(8 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(9 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
        new Block(10 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsDefault.slice(0, 1)),
			],
		    [
        new Block(0 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(2 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(3 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(4 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(5 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(6 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(7 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(8 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(9 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(10 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(11 * blockWidth, 0 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(2 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(3 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(4 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(5 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(6 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(7 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(8 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(9 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(10 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(11 * blockWidth, 1 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(2 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(3 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(4 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(5 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(6 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(7 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(8 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(9 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(10 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(11 * blockWidth, 2 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(2 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(3 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(4 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(5 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(6 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(7 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(8 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(9 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(10 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(11 * blockWidth, 3 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(2 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(3 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(4 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(5 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(6 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(7 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(8 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(9 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(10 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(11 * blockWidth, 4 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(0 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(2 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(3 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(4 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(5 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(6 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(7 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(8 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(9 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(10 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(11 * blockWidth, 5 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(2 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(3 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(4 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(5 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(6 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(7 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(8 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(9 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(10 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(11 * blockWidth, 6 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(2 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(3 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(4 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(5 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(6 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(7 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(8 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(9 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(10 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(11 * blockWidth, 7 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(2 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(3 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(4 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(5 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(6 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(7 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(8 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(9 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(10 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(11 * blockWidth, 8 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(1 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(2 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(3 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(4 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(5 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(6 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(7 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(8 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(9 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(10 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(11 * blockWidth, 9 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(0 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(1 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(2 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(3 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(4 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(5 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(6 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(7 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(8 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(9 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(10 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(11 * blockWidth, 10 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(0 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(1 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(2 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(3 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(4 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(5 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(6 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(7 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(8 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(9 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(10 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(11 * blockWidth, 11 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(0 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(1 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(2 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(3 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(4 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(5 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(6 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(7 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(8 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(9 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(10 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(11 * blockWidth, 12 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 2)),
        new Block(0 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(2 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(3 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(4 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(5 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(6 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(7 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(8 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(9 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 4)),
        new Block(10 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(11 * blockWidth, 13 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(1 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(2 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(3 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(4 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(5 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(6 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(7 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(8 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(9 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(10 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(11 * blockWidth, 14 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(0 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(1 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(2 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(3 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(4 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(5 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(6 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(7 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 1)),
        new Block(8 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(9 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(10 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
        new Block(11 * blockWidth, 15 * blockHeight + blocksTop, blockWidth, blockHeight, colorsMario.slice(0, 3)),
		    ]
		][number];
	};
}

//for (var i = 1; i < 11; i++) {
//    for (var j = 1; j < 11; j++) {
//        blocks.push(new Block(j * blockWidth, i * blockHeight + blocksTop, blockWidth, blockHeight, 3));
//    }
//}